
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>سلة التسوق - {{ store.name }}</title>
  <meta name="description" content="سلة التسوق الخاصة بك">
  {% include "style.liquid" %}
</head>
<body>
  <div class="page">
    {% include "header.liquid" %}

    <main class="container">
      <!-- Cart Header -->
      <div class="cart-header">
        <h1 class="cart-title">سلة التسوق</h1>
      </div>

      <!-- Cart Content -->
      <div class="cart-content" id="cart-content">
        <!-- This will be populated by JavaScript -->
      </div>

      <!-- Empty Cart Template (initially hidden) -->
      <div class="empty-cart-template" style="display: none;">
        <div class="empty-cart">
          <div class="empty-cart-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path>
              <line x1="3" y1="6" x2="21" y2="6"></line>
              <path d="M16 10a4 4 0 0 1-8 0"></path>
            </svg>
          </div>
          <h2>سلة التسوق فارغة</h2>
          <p>لم تقم بإضافة أي منتجات إلى سلة التسوق بعد</p>
          <a href="/" class="btn btn-primary">تصفح المنتجات</a>
        </div>
      </div>

      <!-- Cart Items Template (initially hidden) -->
      <div class="cart-items-template" style="display: none;">
        <div class="cart-layout">
          <!-- Cart Items -->
          <div class="cart-items-section">
            <div class="cart-items-header">
              <h2>المنتجات (<span id="cart-items-count">0</span>)</h2>
              <button class="clear-cart-btn" onclick="clearCart()">مسح السلة</button>
            </div>
            
            <div class="cart-items" id="cart-items">
              <!-- Cart items will be populated here -->
            </div>

            <!-- Continue Shopping -->
            <div class="continue-shopping">
              <a href="/" class="btn btn-outline">
                <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                  <polyline points="15,18 9,12 15,6"></polyline>
                </svg>
                متابعة التسوق
              </a>
            </div>
          </div>

          <!-- Cart Summary -->
          <div class="cart-summary">
            <div class="summary-card">
              <h3>ملخص الطلب</h3>
              
              <div class="summary-row">
                <span>المجموع الفرعي</span>
                <span id="subtotal-amount">0.00 دج</span>
              </div>
              
              <div class="summary-row">
                <span>الشحن</span>
                <span id="shipping-amount">مجاني</span>
              </div>

              <div class="summary-row">
                <span>الضريبة</span>
                <span id="tax-amount">0.00 دج</span>
              </div>

              <hr class="summary-divider">

              <div class="summary-row summary-total">
                <span>المجموع الإجمالي</span>
                <span id="total-amount">0.00 دج</span>
              </div>

              <!-- Promo Code -->
              <div class="promo-code-section">
                <div class="promo-code-input">
                  <input type="text" id="promo-code" placeholder="رمز الخصم">
                  <button onclick="applyPromoCode()">تطبيق</button>
                </div>
                <div class="applied-promos" id="applied-promos">
                  <!-- Applied promo codes will appear here -->
                </div>
              </div>

              <!-- Checkout Button -->
              <button class="checkout-btn" onclick="proceedToCheckout()">
                <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                  <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
                  <circle cx="12" cy="16" r="1"></circle>
                  <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
                </svg>
                إتمام الشراء
              </button>

              <!-- Security Info -->
              <div class="security-info">
                <div class="security-item">
                  <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                    <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
                    <circle cx="12" cy="16" r="1"></circle>
                    <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
                  </svg>
                  <span>دفع آمن ومحمي</span>
                </div>
                <div class="security-item">
                  <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                    <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
                    <circle cx="12" cy="10" r="3"></circle>
                  </svg>
                  <span>شحن سريع وموثوق</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>

    {% include "footer.liquid" %}
  </div>

  <style>
    /* Cart Page Styles */
    .cart-header {
      margin: 2rem 0;
      text-align: center;
    }

    .cart-title {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      color: #333;
    }

    /* Empty Cart */
    .empty-cart {
      text-align: center;
      padding: 4rem 2rem;
      color: #666;
    }

    .empty-cart-icon {
      margin: 0 auto 2rem;
      width: 100px;
      height: 100px;
      opacity: 0.3;
    }

    .empty-cart-icon svg {
      width: 100%;
      height: 100%;
    }

    .empty-cart h2 {
      font-size: 1.5rem;
      margin-bottom: 1rem;
      color: #333;
    }

    .empty-cart p {
      margin-bottom: 2rem;
      font-size: 1.1rem;
    }

    /* Cart Layout */
    .cart-layout {
      display: grid;
      grid-template-columns: 1fr 400px;
      gap: 3rem;
      margin-top: 2rem;
    }

    .cart-items-section {
      min-height: 400px;
    }

    .cart-items-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
      padding-bottom: 1rem;
      border-bottom: 2px solid #f0f0f0;
    }

    .cart-items-header h2 {
      font-size: 1.3rem;
      font-weight: 600;
      color: #333;
    }

    .clear-cart-btn {
      background: none;
      border: 1px solid #ff3b30;
      color: #ff3b30;
      padding: 0.5rem 1rem;
      border-radius: 6px;
      cursor: pointer;
      font-size: 0.9rem;
      transition: all 0.2s ease;
    }

    .clear-cart-btn:hover {
      background: #ff3b30;
      color: white;
    }

    /* Cart Item */
    .cart-item {
      display: grid;
      grid-template-columns: 120px 1fr auto;
      gap: 1rem;
      padding: 1.5rem 0;
      border-bottom: 1px solid #f0f0f0;
      align-items: start;
    }

    .cart-item:last-child {
      border-bottom: none;
    }

    .cart-item-image {
      width: 120px;
      height: 120px;
      border-radius: 8px;
      overflow: hidden;
      background: #f8f9fa;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .cart-item-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .cart-item-details {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }

    .cart-item-title {
      font-size: 1.1rem;
      font-weight: 600;
      color: #333;
      text-decoration: none;
      transition: color 0.2s;
    }

    .cart-item-title:hover {
      color: #ffd23d;
    }

    .cart-item-variant {
      color: #666;
      font-size: 0.9rem;
    }

    .cart-item-price {
      font-size: 1.1rem;
      font-weight: 600;
      color: #333;
      margin-top: 0.5rem;
    }

    .cart-item-controls {
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: 1rem;
    }

    .quantity-controls {
      display: flex;
      align-items: center;
      border: 1px solid #ddd;
      border-radius: 8px;
      overflow: hidden;
    }

    .quantity-btn {
      width: 36px;
      height: 36px;
      background: #f8f9fa;
      border: none;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: background-color 0.2s;
      font-weight: bold;
      color: #333;
    }

    .quantity-btn:hover {
      background: #ffd23d;
    }

    .quantity-btn:disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }

    .quantity-input {
      width: 50px;
      height: 36px;
      border: none;
      text-align: center;
      font-weight: 600;
      color: #333;
      background: white;
    }

    .remove-item-btn {
      background: none;
      border: none;
      color: #ff3b30;
      cursor: pointer;
      font-size: 0.9rem;
      text-decoration: underline;
      transition: color 0.2s;
    }

    .remove-item-btn:hover {
      color: #d70015;
    }

    .continue-shopping {
      margin-top: 2rem;
    }

    .continue-shopping .btn {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
    }

    .continue-shopping .icon {
      width: 16px;
      height: 16px;
    }

    /* Cart Summary */
    .cart-summary {
      position: sticky;
      top: 2rem;
      height: fit-content;
    }

    .summary-card {
      background: #f8f9fa;
      border-radius: 12px;
      padding: 2rem;
      border: 1px solid #e5e5e5;
    }

    .summary-card h3 {
      font-size: 1.3rem;
      font-weight: 600;
      margin-bottom: 1.5rem;
      color: #333;
    }

    .summary-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1rem;
      font-size: 0.95rem;
      color: #333;
    }

    .summary-row:last-child {
      margin-bottom: 0;
    }

    .summary-total {
      font-size: 1.1rem;
      font-weight: 700;
      color: #333;
    }

    .summary-divider {
      border: none;
      height: 1px;
      background: #ddd;
      margin: 1.5rem 0;
    }

    /* Promo Code */
    .promo-code-section {
      margin: 1.5rem 0;
    }

    .promo-code-input {
      display: flex;
      gap: 0.5rem;
      margin-bottom: 1rem;
    }

    .promo-code-input input {
      flex: 1;
      padding: 0.75rem;
      border: 1px solid #ddd;
      border-radius: 6px;
      font-size: 0.9rem;
    }

    .promo-code-input button {
      padding: 0.75rem 1rem;
      background: #333;
      color: white;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 0.9rem;
      transition: background-color 0.2s;
    }

    .promo-code-input button:hover {
      background: #ffd23d;
      color: #333;
    }

    .applied-promo {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0.5rem 0.75rem;
      background: #d4edda;
      color: #155724;
      border-radius: 6px;
      font-size: 0.9rem;
      margin-bottom: 0.5rem;
    }

    .applied-promo button {
      background: none;
      border: none;
      color: #155724;
      cursor: pointer;
      font-weight: bold;
    }

    /* Checkout Button */
    .checkout-btn {
      width: 100%;
      padding: 1rem;
      background: #ffd23d;
      color: #333;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1.1rem;
      font-weight: 600;
      transition: all 0.2s ease;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      margin-bottom: 1.5rem;
    }

    .checkout-btn:hover {
      background: #333;
      color: white;
      transform: translateY(-2px);
    }

    .checkout-btn .icon {
      width: 20px;
      height: 20px;
    }

    /* Security Info */
    .security-info {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .security-item {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.9rem;
      color: #666;
    }

    .security-item .icon {
      width: 16px;
      height: 16px;
      color: #28a745;
    }

    /* Mobile Styles */
    @media (max-width: 768px) {
      .cart-title {
        font-size: 2rem;
      }

      .cart-layout {
        grid-template-columns: 1fr;
        gap: 2rem;
      }

      .cart-item {
        grid-template-columns: 100px 1fr;
        gap: 1rem;
      }

      .cart-item-controls {
        grid-column: 1 / -1;
        flex-direction: row;
        justify-content: space-between;
        margin-top: 1rem;
      }

      .cart-items-header {
        flex-direction: column;
        align-items: stretch;
        gap: 1rem;
      }

      .cart-items-header h2 {
        text-align: center;
      }

      .clear-cart-btn {
        align-self: center;
      }
    }

    @media (max-width: 480px) {
      .cart-title {
        font-size: 1.5rem;
      }

      .cart-item-image {
        width: 80px;
        height: 80px;
      }

      .cart-item {
        grid-template-columns: 80px 1fr;
      }

      .summary-card {
        padding: 1.5rem;
      }

      .quantity-controls {
        transform: scale(0.9);
      }
    }
  </style>
</body>
</html>