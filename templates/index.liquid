<!DOCTYPE html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ store.meta.title | default: store.name }}</title>
    {% if store.meta.description %}
      <meta name="description" content="{{ store.meta.description }}">
    {% endif %}
    {% if store.meta.keywords %}
      <meta name="keywords" content="{{ store.meta.keywords }}">
    {% endif %}
    {% include "style.liquid" %}
  </head>
  <body>
    <div class="page">
      {% include "header.liquid" %}

      <!-- Hero Section -->
      <section class="hero">
        <div class="container">
          <div class="hero-content">
            <h1 class="hero-title">
              {% if store.hero_title %}
                {{ store.hero_title }}
              {% else %}
                مرحباً بكم في {{ store.name }}
              {% endif %}
            </h1>
            <p class="hero-description">
              {% if store.hero_description %}
                {{ store.hero_description }}
              {% else %}
                اكتشف مجموعتنا المميزة من المنتجات عالية الجودة
              {% endif %}
            </p>
            <div class="hero-actions">
              <a href="/shop" class="btn btn-primary">تسوق الآن</a>
            </div>
          </div>
          {% if store.hero_image %}
            <div class="hero-image">
              <img src="{{ store.hero_image }}" alt="Hero Image" />
            </div>
          {% endif %}
        </div>
      </section>

      <!-- Featured Products Section -->
      <main class="container">
        <section class="featured-products">
          <div class="section-header">
            <h2 class="section-title">المنتجات المميزة</h2>
            <p class="section-description">اختيارنا المميز من أفضل المنتجات</p>
          </div>

          {% if featured_products.size > 0 %}
            <div class="products-grid">
              {% for p in featured_products %}
                {% render "product-card.liquid", product: p %}
              {% endfor %}
            </div>
            
            <div class="section-footer">
              <a href="/shop" class="btn btn-outline">عرض جميع المنتجات</a>
            </div>
          {% else %}
            <div class="empty-state">
              <div class="empty-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                  <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path>
                  <line x1="3" y1="6" x2="21" y2="6"></line>
                  <path d="M16 10a4 4 0 0 1-8 0"></path>
                </svg>
              </div>
              <h3>لا توجد منتجات مميزة حالياً</h3>
              <p>تابعونا قريباً لإضافة منتجات جديدة ومميزة!</p>
            </div>
          {% endif %}
        </section>

        <!-- Categories Section (Optional) -->
        {% if store.featured_collections.size > 0 %}
          <section class="featured-categories">
            <div class="section-header">
              <h2 class="section-title">تصفح الفئات</h2>
              <p class="section-description">اختر الفئة التي تبحث عنها</p>
            </div>
            
            <div class="categories-grid">
              {% for category in store.featured_collections %}
                <a href="/collections/{{ category }}" class="category-card">
                  <div class="category-info">
                    <h3>{{ category }}</h3>
                  </div>
                </a>
              {% endfor %}
            </div>
          </section>
        {% endif %}
      </main>

      {% include "footer.liquid" %}
    </div>
  </body>
</html>